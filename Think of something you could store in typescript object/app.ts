interface item{
    name:string
    price:number
}
// create two objects
const book: item ={
    name: 'Essential typescrist',
    price:450
}
const apple: item ={
    name:'apple'
    price:200
}
console.log(`book name: ${book.net}, price: $${book.price}`)
console.log(`apple name: ${apple.net}, price: $${apple.price}`)
